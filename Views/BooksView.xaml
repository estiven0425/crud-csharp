<UserControl d:DesignHeight="450" d:DesignWidth="800"
             mc:Ignorable="d"
             x:Class="crud_csharp.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
                    Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Content="Return"
                    Command="{Binding NavigationMainCommand}"
                    Grid.Column="0"
                    Grid.Row="0"/>
            <TextBox Grid.Column="1"
                    Grid.ColumnSpan="4"
                     Grid.Row="0"
                     Height="30"
                     Margin="10,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>
            <Button Content="Search"
                    Command="{Binding GetBookByTitleCommand}"
                    Grid.Column="5"
                    Grid.Row="0"/>
        </Grid>
        <Grid Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    Margin="0,0,0,10">
            <Button Content="Add book"
                    Command="{Binding NavigationNewBookCommand}"/>
        </Grid>
        <Grid Grid.Row="2">
            <ListView ItemsSource="{Binding Books}"
                      SelectedItem="{Binding SelectedBook}">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseDoubleClick">
                        <b:InvokeCommandAction Command="{Binding NavigationBookCommand}"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn DisplayMemberBinding="{Binding Title}"
                                        Header="Title"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Country}"
                                        Header="Country"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Author.Name}"
                                        Header="Author"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Genre.Name}"
                                        Header="Genre"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Price}"
                                        Header="Price"/>
                    </GridView>
                </ListView.View>
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="IsHitTestVisible"
                                Value="False"/>
                    </Style>
                </ListView.Resources>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
